Bug fixes and improvements

- Settings lock is now available on any website, not just predefined platforms
- Redesigned the lock confirmation as a focus commitment modal ("Commit to staying focused?" with Lock in / Not now, and a shortcut to change the unlock wait time in Settings)
- Renamed the manual CSS selector button to compact "CSS +" so it sits beside "Click to hide element"
- Fixed misleading "needs to initialise" copy on unsupported pages (e.g. chrome://) — the popup now shows a clear message instead
- Updated footer attribution to digitalhabits.org
